name: musicstore
metadata:
  template: musicstore@0.0.1
services:
  web:
    project: ./src/MvcMusicStore
    language: docker
    host: containerapp
    docker:
      path: ./Dockerfile
      context: .
  musicgen:
    project: ./src/musicgen
    language: docker
    host: containerapp
    docker:
      path: ./Dockerfile
      context: .
